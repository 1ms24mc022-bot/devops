pipeline {
    agent any

    tools {
        jdk 'jdk21'   // Must match the name configured in Jenkins
    }

    stages {
        stage('Checkout') {
            steps {
                // Clone your GitHub repo
                git branch: 'master', url: 'https://github.com/1ms24mc022-bot/devops'
            }
        }

        stage('Compile') {
            steps {
                echo 'Compiling Java program...'
                sh 'javac Hello_RIT.java'
            }
        }

        stage('Run') {
            steps {
                echo 'Running Java program...'
                sh 'java Hello_RIT'
            }
        }
    }

    post {
        success {
            echo 'Build and execution successful!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}
